# Subagent profiles — superseded

This draft is superseded by `docs/agent-catalog.md`.

The catalog plan deletes `small` / `medium` slots, returns a session id from `spawn_agent`, and ends the parent run after a successful spawn. Do not implement this file.
